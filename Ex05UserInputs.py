# Input() = A Function that prompts the user to enter the data. This function waits the user to make an entry and
# returns that value. The value can be stored into a variable.
#All inputs that come into
name = input("Enter the Name: ")
print("The Name entered is " + name)
age = int(input("How old are you?: "))
print(f"Your are {age} years old")
